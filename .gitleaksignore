# Known-benign gitleaks findings.
#
# Every entry here must be a synthetic fixture, never a real credential that
# was rotated — a rotated secret still belongs out of history, not on an
# allowlist. Entries are commit-scoped fingerprints, so a genuine new secret
# in the same file still fails the scan.
#
# `rmail-core/src/ai/redact/tests.rs` is the PII/secret redaction firewall's
# own test suite. Its job is proving that credential-shaped strings get
# tokenized before anything leaves the machine, which means it necessarily
# contains credential-shaped strings. The alternative is a secret detector
# nobody can test.
#
# Do not quote the offending literal in this comment: an earlier revision did,
# and the allowlist file became a finding of its own.
#
# The in-file `gitleaks:allow` annotation covers working-tree scans; these
# fingerprints cover `gitleaks detect`'s history scan, which reads the blob as
# it was committed and so cannot see a later-added annotation.
5b818403b31f612f791373c478c9a4515c9b5143:rmail-core/src/ai/redact/tests.rs:generic-api-key:197
6b71dfd4d80e5916e78da862f8f672673c2f2d83:rmail-core/src/ai/redact/tests.rs:generic-api-key:197

# The revision of this file that quoted the fixture literal (fixed in the
# next commit; the blob keeps it, so the fingerprint stays).
5dec4cfee8026929f7c90a19f9e3c4ed9b0ce31b:.gitleaksignore:generic-api-key:11
